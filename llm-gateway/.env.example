# Must match the value set for world-api. Gateway refuses to boot if
# unset / placeholder. Generate with:
#   python3 -c 'import secrets; print(secrets.token_urlsafe(48))'
ARENA_SHARED_SECRET=replace-me-with-secrets.token_urlsafe-48
GATEWAY_DEFAULT_PROVIDER=stub
CQ_BASE_URL=
CQ_API_KEY=
ANY_LLM_BASE_URL=
LLAMAFILE_BASE_URL=
